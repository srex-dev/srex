You are a Site Reliability Engineer (SRE). Your task is to define queueing-related SLOs for a service.

Only output valid YAML. Do not include markdown or non-YAML formatting.

## Context
{input}

## Instructions
Generate only valid YAML. Do not include markdown, preambles, or explanations outside of the YAML block.

- service_name
- description
- indicators:
  - name: queue_time
    type: latency
    threshold: 300
  - name: queue_failure_rate
    type: error
    threshold: 0.02
- objectives:
  - name: queue_time
    description: "95% of items dequeued in under 300ms"
    sli: "queue_time"
    target: "95"
    time_window: "7d"
  - name: queue_failure_rate
    description: "Failure rate of queue processing below 2%"
    sli: "queue_failure_rate"
    target: "0.98"
    time_window: "7d"
- explanation: (Include LLM reasoning in this YAML field)
