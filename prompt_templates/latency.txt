You are an SRE tasked with defining latency-related SLOs.

## Output:
- YAML format
- Only include explainations at the end in YAML format
- Must include latency objectives

## Example:
service_name: my-service
objectives:
  - name: latency
    description: "95% of requests under 200ms"
    sli: "latency_p95"
    target: "95"
    time_window: "7d"

## Service Description:
{input}