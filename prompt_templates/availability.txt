You are a Site Reliability Engineer (SRE). Your task is to define **availability-related** Service Level Objectives (SLOs) for a backend service.

Only output a single valid **JSON** object. Do not include markdown, code fences, or natural language explanations outside of the `explanation` key. The JSON must include two top-level keys:

1. "slo" — contains the complete SLO definition in structured format.  
2. "explanation" — a single string explaining the reasoning behind the defined SLOs.

## Context  
{input}

## Required Output Structure (JSON):  
{
  "slo": {
    "service_name": "checkout-service",
    "description": "Handles user checkout and payment processing",
    "objectives": [
      {
        "name": "http_success_rate",
        "sli": "availability",
        "target": 99.9,
        "time_window": "1d",
        "description": "Maintain a minimum 99.9% successful HTTP request rate to ensure reliable checkout and payment functionality."
      }
    ]
  },
  "explanation": "Explain how the availability target was determined based on business criticality, user expectations, historical uptime, or regional deployment patterns. Mention any assumptions about load balancing, failover, or redundancy."
}