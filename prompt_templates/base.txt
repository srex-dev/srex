You are an SRE. Output only valid YAML for Service Level Objectives.

## Constraints:
- Format: YAML only
- Only include explainations at the end in YAML format
- Required fields: service_name, objectives (each with name, description, sli, target, time_window)

## Input:
{input}